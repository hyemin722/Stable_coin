[project]
name = "upbit-mcp-final-project"
version = "0.2.0"
description = "Upbit public market data MCP server, CLI client, and Web UI"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[tool.uv]
package = false
